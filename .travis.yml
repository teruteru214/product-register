sudo: required

services: docker

before_install:
  - docker-compose up --build -d
  - docker login -u "_" -p "ddd47a5d-310a-4734-9759-837a1678ab10" registry.heroku.com

script:
  - docker-compose exec --env 'RAILS_ENV=test' web rails db:create
  - docker-compose exec --env 'RAILS_ENV=test' web rails db:migrate
  - docker-compose exec --env 'RAILS_ENV=test' web rails test

deploy:
  provider: script
  script:
    docker build -t register.heroku.com/teru-product-register/web -f Dockerfile.prod .;
    docker push register.heroku.com/teru-product-register/web;
    heroku run --app teru-product-register rails db:migrate;
  on:
    branch: master
